#!/bin/bash

set -e

DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )
cd "$DIR/.."

npm install
export PATH="node_modules/.bin:node_modules/hubot/node_modules/.bin:$PATH"

. ~/config/set-env.sh

exec node_modules/.bin/hubot --name "NLB" --adapter slack "$@"
